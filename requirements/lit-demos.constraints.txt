# Pinned dependencies for running LIT demos consistently across platforms.
# These pins are chosen to work with Python 3.12 on macOS (arm64), Linux, and Windows.
# Platform extras are installed by the Makefile (tensorflow-macos/tensorflow-metal on macOS arm64; tensorflow on others).

# Core
numpy==1.26.4
scipy==1.11.4
pandas==2.1.4
pyarrow==14.0.2
h5py>=3.10,<4

# LIT and ecosystem
lit-nlp==1.3.1
tensorflow-datasets>=4.9,<5
transformers>=4.40,<5
sentencepiece>=0.1.99,<0.2

# Keras API used by TF 2.16.x
# tf-keras will be installed as platform extra (unversioned here; pinned in Makefile for macOS)
