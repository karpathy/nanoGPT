# Cosmic Ray configuration for ml_playground (v8 schema)

[cosmic-ray]
module-path = "ml_playground"
timeout = 120.0
excluded-modules = []
# Kept quiet-ish; our wrapper may invoke via `uv run`.
test-command = "pytest -q -n auto tests"

[cosmic-ray.distributor]
name = "local"
